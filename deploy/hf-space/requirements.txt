# Hugging Face ZeroGPU Space requirements for the MedRAX demo.
#
# ZeroGPU is Gradio-only and requires Torch >= 2.8 (CUDA wheels, no nvcc).
# See https://huggingface.co/docs/hub/en/spaces-zerogpu for supported versions.
#
# Option A (recommended): install MedRAX from the upstream repo so the medrax/
# package and tools come along, then copy interface.py + main.py into the Space.
#   git+https://github.com/bowang-lab/MedRAX.git
#
# Option B: vendor apps/api/services/medagent/ into the Space and pin the
# subset below. Keep this in sync with that service's pyproject.toml.

spaces
gradio>=5.0.0
gradio_client>=0.2.0
torch>=2.8.0
torchvision>=0.20.0
torchxrayvision>=0.0.37
transformers>=4.44.0
tokenizers>=0.10.0
sentencepiece>=0.1.95
accelerate>=0.12.0
peft>=0.2.0
bitsandbytes>=0.43.0
langchain-core>=0.1.0
langchain-community>=0.0.10
langchain-openai>=0.0.2
langchain-anthropic>=0.0.2
langgraph>=0.0.10
python-dotenv>=0.19.0
pydantic>=2.0.0
Pillow>=8.0.0
opencv-python>=4.8.0
scikit-image>=0.18.0
numpy>=1.19.0
einops>=0.3.0
timm>=0.5.0
openai>=0.27.0
