import 'package:flutter/material.dart';

import 'forgot_password_view.dart';

/// Legacy radiologist password-reset entry point.
class ForgotPasswordRadioView extends StatelessWidget {
  const ForgotPasswordRadioView({super.key});

  @override
  Widget build(BuildContext context) => const ForgotPasswordView();
}
